<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Airdrop</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <nav class="bg-white shadow-lg">
        <div class="max-w-6xl mx-auto px-4">
            <div class="flex justify-between">
                <div class="flex space-x-7">
                    <div class="flex items-center py-4">
                        <a href="/" class="text-lg font-semibold">Roblog</a>
                    </div>
                    <div class="hidden md:flex items-center space-x-1">
                        <a href="/" class="py-4 px-2 text-gray-500 font-semibold hover:text-blue-500 transition duration-300">CV</a>
                        <a href="/airdrop" class="py-4 px-2 text-blue-500 border-b-4 border-blue-500 font-semibold">Airdrop</a>
                        <a href="/blog" class="py-4 px-2 text-gray-500 font-semibold hover:text-blue-500 transition duration-300">Blog</a>
                        <a href="/project" class="py-4 px-2 text-gray-500 font-semibold hover:text-blue-500 transition duration-300">Project</a>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-8 text-center">Airdrop Aktif</h1>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% airdrops.forEach(function(airdrop) { %>
                <div class="bg-white rounded-lg shadow-lg overflow-hidden">
                    <div class="p-6">
                        <h2 class="text-xl font-semibold mb-2"><%= airdrop.project %></h2>
                        <div class="space-y-2">
                            <p class="text-sm">
                                <span class="font-medium">Status:</span>
                                <span class="px-2 py-1 rounded text-sm <%= airdrop.status.toLowerCase() === 'active' ? 'bg-green-100 text-green-800' : 'bg-yellow-100 text-yellow-800' %>">
                                    <%= airdrop.status %>
                                </span>
                            </p>
                            <p class="text-sm"><span class="font-medium">Reward:</span> <%= airdrop.reward %></p>
                            <p class="text-sm"><span class="font-medium">Deadline:</span> <%= airdrop.deadline %></p>
                            <p class="text-sm"><span class="font-medium">Source:</span> <%= airdrop.source %></p>
                        </div>
                        <div class="mt-4">
                            <a href="<%= airdrop.link %>" target="_blank" class="inline-block bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition duration-300">
                                Ikuti Airdrop
                            </a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>

        <% if (airdrops.length === 0) { %>
            <div class="text-center py-8">
                <p class="text-gray-500">Tidak ada airdrop aktif saat ini.</p>
            </div>
        <% } %>
    </div>

    <footer class="bg-white shadow-lg mt-8">
        <div class="container mx-auto px-4 py-6 text-center text-gray-600">
            <p>Data airdrop diperbarui setiap 6 jam</p>
        </div>
    </footer>
</body>
</html>